# 📦 LX_Pro_Rebuild 项目交接文档

> 最后更新：2026-09-23（**外观打磨**：色彩统一 / 图表调色 / 字号阶梯 / 泛光 + 液态玻璃）
> 用途：**新会话快速接手**
> ⚠️ 新会话请**先读这份文档**，再读 `docs/10_空间重组设计思想.md`
> ⚠️ 外观规范详见 **`docs/15_外观打磨规范.md`**（色板 token / 字号阶梯 / 泛光 / 液态玻璃）

---

## 一、项目定位（一句话）

> 把 **LX-Pro-Music（React Native 音乐播放器）** 的全部界面与交互，用**真 3D 空间语言**（Three.js）重建成一个 HTML 原型。
> **只做 UI 与交互，不接真实功能。**

**核心美学**：灰黑忧郁 · inmyfeelings · ins 冷淡 · 电影感 · **空间纵深** · 沉浸感
**当前色调**：**中性灰黑 + 冷调微光**（底色 `#08090C`，强调 `#7A828C`）

> ⚠️ **色彩订正（2026-09-22）**：本文旧版曾写「冷蓝色调 `#0A0D14` + `#6E8FAD`」，**与代码不符**。
> 代码里的真实值是中性灰（见第七节）。观感偏冷蓝主要来自**卡片内部配色**（歌单封面 `#3E4E58` 一类）
> 和**曲目主题色**（`TRACKS[].accent`），主色基建是灰的。

**最终去向（项目作者 2026-09-22 确认）**：本原型用于**验证效果**，
最终 App 要用 **Android 原生重写**（不是 WebView 直接装原型）。
因此文档里的参数/色值需要可被原生实现忠实复刻——见第十四节。

---

## 二、工作区结构

```
~/Hoper/LX_Pro_Rebuild_UI/          ★ 本项目工作区（1.2M）
├── prototype/
│   ├── app.html                              ★★★ 主文件（3150 行）唯一在维护的原型
│   ├── three.min.js                          Three.js r160（669KB，本地，必须同目录）
│   └── legacy/                               ⛔ 早期迭代归档（不再维护）
│       ├── README.md                         归档说明：每个文件的定位与废弃原因
│       ├── index.html / home.html            无外部依赖，可独立打开
│       ├── home_space.html / home_sphere.html
│       └── space.html / space2.html / space_pro.html
│           ↑ 前 4 个引用 ../three.min.js（归档时改过路径）
├── docs/                                     01~15 策划与规范文档
├── tools/                                    外观验证工具（2026-09-23 新增）
│   ├── wd_verify.py                          geckodriver 真实渲染验证（控制台+状态）
│   ├── make_diag_copy.py                     生成带探针的 _diag.html
│   └── verify_visual.py                      4 变体逐像素对比（证明效果生效）
└── backups/
    └── app_v2_外观打磨前_20260923.html       本次打磨前的快照
```

> ⚠️ **路径订正**：旧版本文档全篇写 `/sdcard/Hoper/LX_Pro_Rebuild/`（那是手机上 Operit 环境的路径）。
> 当前工作区：`~/Hoper/LX_Pro_Rebuild_UI/`（开发机为 Lubuntu）。

### 打开方式
用**支持 WebGL 的浏览器**打开 `prototype/app.html`（必须与 `three.min.js` 同目录）。
> ⚠️ 某些 APP 内置 WebView 会拦截本地脚本 → 会卡在加载；**换浏览器**即可。
> 本机 `firefox` 可用；但本机内存小（约 3.7GB），跑浏览器前先看 `free -h`。

### 参考项目（两条线索，别搞混）

**① 界面调研来源**（React Native 旧版，供 `docs/08` 参考）
- 本机路径：`~/Hoper/LX-Pro-Music`（**注意是连字符**，不是下划线）
- GitHub：`AsrnkHoper/LX-Pro-Music`

**② 真正的目标项目**（★ Android 原生重写的落地处）
- 本机路径：`~/Hoper/LX_Pro_Rebuild`（**256K，12 份文档，无代码**）
- 它是**策划工作区**，与本文档所在的 `LX_Pro_Rebuild_UI`（**UI 原型工作区**）是**两个不同目录**
- 已拍板：`Kotlin + Jetpack Compose + Media3`、包名 `com.lxpro.music`、minSdk 33
- 视觉第一优先：**液态玻璃（Haze）+ 空间深度**
- ⚠️ **两个工作区目前互不引用**——见第十四节

```
~/Hoper/
├── LX_Pro_Rebuild/            ★ 原生主项目（策划文档 01~11，无代码）
│   ├── 02-策划设计.md          决策记录 + 第十二节「给未来 AI 的需求提示词素材」
│   ├── 11-视觉设计体系.md      ★ 已有设计令牌 / 字号阶梯 / 动效令牌 / 玻璃规范
│   └── 06-开发路线图与里程碑.md  M0~M4 里程碑
├── LX_Pro_Rebuild_UI/         ★ 本工作区（UI 原型）
│   ├── prototype/app.html      Three.js 原型
│   └── docs/01~11              原型策划与规范
└── LX-Pro-Music/              参考（RN 旧版，调研来源）
```

---

## 三、当前进度总览

**9 个空间 / 38 种控件 / 6 张主页卡 / app.html 9671 行 / 261 项测试**

> ⚡ **2026-09-23 外观打磨**（本次会话）：色彩体系统一（清 130+ 处冷蓝残留）、
> 图表改冷灰调、字号收敛为 10 档阶梯、手写 Bloom 泛光、液态玻璃折射。
> 详见 `docs/15_外观打磨规范.md` 与第九·六节。

| 空间 | 内容 | 状态 |
|---|---|---|
| 主页 | 球面 **6 卡**（az 0/60/120/180/240/300） | ✅ |
| `now` 当前播放 | 9 控件：歌词/封面/信息/控制/按钮行/更多菜单/播放设置 | ✅ |
| `playlist` 我的歌单 | 5 控件：搜索/分类/歌单网格/歌曲列表/导航条 | ✅ |
| `stats` 听歌统计 | 13 控件：**12 区块**（6 图表 + 3 metrics + 排行 + 最近） | ✅ |
| `search` 搜索 | 4 控件：搜索栏 + **4 类型切换** + 结果列表 | ✅ |
| `settings` 设置 | 10 控件：**7 分区** | ✅ |
| `songlist` / `album` / `artist` | 头图卡 + 歌曲列表（详情页） | ✅ |
| `download` 下载管理 | 3 控件（简版） | 🟡 |

### 本次会话（2026-09-22 ~ 23）完成的工作

| # | 工作 | 说明 |
|---|---|---|
| 1 | 归档遗留原型 | 7 个早期 HTML → `prototype/legacy/` |
| 2 | 修 bug：黑屏 | `maxScroll` 未定义 → buildPage 中断；加绘制异常隔离 |
| 3 | 长列表虚拟滚动 | 523 首贴图 119.85MB → 2.86MB；**6 种控件可滚** |
| 4 | 导航手段 | 搜索直达 / 导航条 / 最近访问置顶 / 惯性 / 吸附 |
| 5 | 修 bug：导航条 NaN | `hitRegion` 未返回 `fy` |
| 6 | 统计页 12 区块 | 6 种图表 + 3 metrics |
| 7 | **接入真实数据** | 3208 首 / 40 歌单 / 649 播放事件（LX-Pro 备份） |
| 8 | **设置页 7 分区** | 主题/播放/歌词/下载/列表/备份/版本 |
| 9 | **专辑/歌手/歌单详情** | `headcard` 控件 + **空间导航栈**（逐级返回） |
| 10 | **搜索页 4 类型** | 本地全量搜索（3208 首） |
| 11 | 回归测试套件 | **223 项断言**，`node test/run.js` |
| 12 | 文档体系 | docs/12 部署、docs/13 重构方案、docs/14 覆盖审计 |

### ⚡ 玻璃化与形态重构（最后阶段）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **玻璃化地基** | PMREM 程序化环境贴图 + 光照 4 盏 + `MeshPhysicalMaterial`（clearcoat/iridescence） | ✅ 已上线，**性能不卡**（用户确认） |
| **溶解式底板** | `destination-out` 四边线性擦除 + 四角加强；`PlaneGeometry` 替代 `BoxGeometry` | ✅ |
| **音频响应** | `AUDIO` 状态 + `pulseFromAudio`（BPM 92 伪节拍）→ 粒子呼吸 | ✅ **用户认可** |
| ~~星座光弧~~ | 卡片间贝塞尔弧线 | ❌ **用户否决**（"像 bug 一样突兀"） |
| 泛光 Bloom | 需手写 shader（`three.min.js` 无 examples） | ⬜ 未做 |

### ⚠️ 未决问题（新会话优先看）

1. **卡片边界是否已彻底消除？** —— 用户曾反馈"卡片还是有边界"。已修三个层次：
   ①`BoxGeometry` 侧边 → `PlaneGeometry`；②圆形擦除 → 四边线性；
   ③**新增 SDF 软边**（液态玻璃，`docs/15` §5.2）→ **但用户尚未在真机确认**
2. **边界的来源待确认**：可能是**卡片外轮廓**（底板问题，已修三层）或**卡片内部矩形**
   （如 `CARD.now` 里的封面框/进度条 —— 若如此需改 `CARD.*` 绘制函数）
3. ~~泛光未做~~ → ✅ **已完成（2026-09-23）**：手写 5-pass 管线，threshold 0.82，
   见 `docs/15` §四。**待真机确认观感与性能**
4. **外观打磨待真机确认**（2026-09-23）：色彩/字号已客观验证（测试 + 逐像素对比），
   但 PBR（泛光/液态玻璃）的**观感与帧率**需项目作者在真机上确认 —— 见 `docs/15` §七

---

## 四、app.html 架构地图

```
┌─ HTML 结构 ────────────────────────────────────┐
│ #scene            3D 画布                        │
│ .fx-grade         冷暖分级层                      │
│ .fx-vignette      暗角                          │
│ .fx-leak          柔光斑（11s 呼吸）              │
│ .fx-grain         胶片颗粒（0.7s 抖动）           │
│ .fx-wipe          转场黑场                       │
│ .indicators       屏幕边缘方向箭头                │
│ .top              顶部问候（随时间段变化）         │
│ .crumb            子空间栏目名                    │
│ .toast            提示条                        │
│ .back             ← 返回主页按钮                 │
│ .bottom           索引栏 + 迷你播放条             │
│ .sheet            搜索输入面板（真实 DOM 输入）    │
│ .np               沉浸式播放器 DOM（⛔ 停用）      │
└────────────────────────────────────────────────┘
```

### JS 分块（真实行号，2026-09-23 外观打磨后校订 · app.html 共 9671 行）

> ⚠️ 行号由 `prototype/test/linemap.py` 一键生成，改代码后重跑并更新本表。

| 区块 | 行号 | 内容 |
|---|---|---|
| 基础 | L400~4009 | `FONT`(L400) / **`TS` 字号阶梯(L413)** |
| **真实数据** | **L465~4499** | **`REAL`**（真实备份内联）：overview/hourly/daily/radar/topSingers/topSongs/sources/**playlists（含 songs）**/allSongs(3208)/recent/topByDuration/dailySeries/**albums/artists** |
| 基础·渲染器 | L4011~4680 | renderer / scene / camera（L4019）/ **光照系统** / **`BLOOM` 泛光后处理(L4115)** |
| **液态玻璃** | **L4383~4535** | **`LIQUID` 参数(L4383) / `applyLiquidGlass`(L4392) / `makeLiquidGlassFace`(L4485) / `LIQUID_FAILED` 安全网(L4505)** |
| 数据·卡片 | L4522~4682 | `FEATURES`(L4522，6 张) / `SPOTS`(L4532) |
| 数据·空间 | L4683~5085 | **`PAGES`**（**9 个空间**） |
| 配色 | L5086 | `PALETTES` |
| 绘图工具 | L5092~5174 | `roundRect` / `drawPanelBg` / **`drawCardBg`（溶解式）** / `hexA` / `wp` |
| **控件绘制** | **L5214~6791** | **`DRAW`：38 种控件** |
| 控件高度 | L6792~6824 | `chipsRows` / `widgetHeight` |
| 主页卡片绘制 | L6825~7276 | `CARD_PPM`(L6825) / `CARD`(L6960) |
| 贴图 | L7277~7293 | `HALO_TEX` / `makeSoftDotTexture` |
| 主页卡片创建 | L7294~7348 | `mainGroup` / `R=5.6`(L7297) / `mainPanels`（**PlaneGeometry** + **液态玻璃**） |
| ⛔ 环绕菜单墙（死代码） | L7349~7558 | `MENUS` / `buildMenuWall`（调用被注释 L7488） |
| **长列表核心** | **L7559~8037** | `LIST_BUF` / `listRows` / `scrollList` / `tickListInertia` / `tickListSnap` / `navSeek` |
| 最近访问 | L7602~8036 | `RECENT_MAX` / `touchRecent` / `orderByRecent` / `reorderGrid` |
| 子空间控件系统 | L7560~8246 | `pageByKey` / `redrawWidget` / `makeWidgetMesh`(L8038) / `buildPage` / `activatePage` |
| 播放器数据 | L8247~8343 | `TRACKS` |
| ⛔ 3D 黑胶（死代码） | L8344~8385 | `coverGroup` / `layoutVinyl` |
| 播放状态 | L8386~8448 | `NP` / `track()` / `fmt()` |
| 共用 | L8449~8469 | `setPlaying` / `syncMini` |
| ⛔ DOM 播放器（死代码） | L8470~8509 | `loadTrack` / `openPlayer` / `closePlayer` |
| **空间播放器** | L8510~8618 | `nowWidgets` / `switchTrack` / `nowTick` |
| **音频响应** | L8619~8707 | 星空 / 尘埃 / **`AUDIO` / `pulseFromAudio` / `updateAudio` / `applyAudioVisual`** |
| 状态与相机 | L8708~8760 | `mode` / `CAM` / `camCfg` |
| 射线 / 交互 | L8761~8982 | `raycastWidget` / `hitRegion` / `onDown` / `onMove` / `onUp` |
| 控件行为 | L8983~9235 | `updateSongList` / `syncPlaylistSet` / **`handleRegion`** |
| 搜索面板 | L9236~9297 | `openSheet` / `submitSearch` / **`localSearch` / `applySearch`** |
| **导航栈 + 转场** | L9298~9441 | `wipe` / `playEnter` / **`spaceStack`** / **`gotoSpace`** / `enterSection` / `exitSection` |
| 定位 | L9442~9528 | `lookAtCard` / `rebuildIndex` / `updateIndicators` |
| 动画循环 | L9529~9649 | `animate()`（含 `tickListInertia` / `tickListSnap` / `updateAudio` / **`BLOOM.render`**） |
| UI | L9650~9671 | `toast` / 迷你条按钮绑定 |

> **维护提示**：改代码后请顺手更新上表行号，否则下一轮接手会踩同样的坑。

---

## 五、空间设计语言（务必遵守）

> 详细见 `docs/10_空间重组设计思想.md`

**三种空间形态：**
| 形态 | 用于 | 说明 |
|---|---|---|
| **A 球面环** | 主页 | 卡片环绕球面，转动浏览 |
| **B 竖列/列表空间** | 内容页 | 面板竖排，纵向转动 |
| **C 多焦点空间** | 播放页 | 正面/左侧/右侧分置，转动切换焦点 |

**6 条设计原则：**
1. 一切皆在空间里（没有"页面"，只有"空间"）
2. 转动视角 = 获取信息
3. 裸元素优先（内容不加卡片框）
4. 统一转场语言（卡片冲向镜头 → 压黑 → 新空间飞入）
5. 交互元素正对镜头（可点的不倾斜）
6. 氛围由相机统一提供（元素不各自漂浮）

**⛔ 绝对不要做的**：
- 把空间改回扁平列表
- 用 Tab 分页代替"角度"
- 用弹窗代替"环绕面"
- 1:1 复刻 LX-Pro 的页面结构（它有单屏限制，我们没有）

---

## 六、两套坐标 / 参数系统

### A. 主页卡片（球面坐标）

```js
FEATURES = [ { key, title, sub, desc, w, h }, ... ]   // 6 张（L1209）
SPOTS    = [ { az, el }, ... ]                        // 与 FEATURES 一一对应（L395）
R = 6.5                                               // 球半径（越小越近越大，L1797）

// 位置计算
px = cos(el)*sin(az); py = sin(el); pz = -cos(el)*cos(az)
mesh.position.set(px*r, py*r, pz*r)   // r = R*(0.92+rand*0.16)
```

**当前 6 张卡**（2026-09-22 删「播放历史」，方位按 **60° 均匀**重排）：

| 卡片 | key | az | el | 尺寸 |
|---|---|---|---|---|
| 当前播放（焦点卡） | `now` | 0 | 0 | 4.0×2.85 |
| 搜索 | `search` | 60 | +4 | 3.9×2.5 |
| 我的歌单 | `playlist` | 120 | −4 | 3.9×2.5 |
| 听歌统计 | `stats` | 180 | +4 | 3.9×2.5 |
| 下载管理 | `download` | 240 | −3 | 3.9×2.5 |
| 设置 | `settings` | 300 | +3 | 3.9×2.5 |

> ⚠️ **订正**：旧版写「含 love 收藏卡」——实际 `FEATURES` **不含** `love`。
> `CARD.love` 绘制函数仍在（L1651）但已无卡片调用 → 属死代码（见第九节）。

**卡片绘制**：`CARD[key](g, W, H, f)`，画布 `w*CARD_PPM`（CARD_PPM=200）
- 通用工具：`cardEn`（英文小标题）/ `cardCn`（中文标题）
- `CARD` 现有 **8 个键**：`search` / `playlist` / `stats` / `now` / `history` / **`love`(死)** / `download` / `settings`

### B. 子空间控件（两种布局）

**布局 1：竖列模式**（搜索/统计/下载/设置/历史 —— 未标 `az`/`sf` 的空间）
```js
PAGE_W = 5.0      // 控件默认宽度（L1993）
PAGE_R = 8.4      // 球半径（L1994）
PPM    = 130      // 每世界单位像素（L1992）
gap    = 0.55     // 控件间距（L2113）
// 控件按高度自动竖排，居中
```

**布局 2：自由布局**（播放页 `now` / 歌单页 `playlist` —— 标了 `sf`/`sv` 或 `az`/`el`）
```js
PAGES.now = {
  cam: { yawMax, pitchMax, sensX, sensY },   // 该空间专属相机
  radius: 7,
  widgets: [
    { type:'lyric3d', sf:0.29, sv:0.47, fw:0.52, fh:0.32, pscale:1.7, tilt:0.32, bare:true, float:0 },
    ...
  ]
}
```

### 控件参数速查

| 参数 | 含义 |
|---|---|
| `sf` / `sv` | 面板中心的**屏幕比例**（0~1，0.5=正中） |
| `fw` / `fh` | 宽/高占**屏幕比例**（fw 相对屏宽，fh 相对屏高） |
| `w` / `h` | 直接指定世界尺寸（竖列布局用） |
| `square` | 高度=宽度（正方形） |
| `pscale` | **排版缩放**（内容放大，面板不变） |
| `bare` | **裸元素**（PlaneGeometry，无底板无厚度） |
| `tilt` | **固定倾斜**弧度（不跟随相机，产生透视） |
| `float` | 漂浮幅度，`0`=完全不漂（保持间距稳定） |
| `az` / `el` | 球面坐标定位（与 sf/sv 二选一） |

### C. 25 种控件（`DRAW` 对象，L1628~2869）

| 类型 | 用途 | 可点区域 kind | PAGES 在用 |
|---|---|---|---|
| `search` | 搜索栏（输入框+按钮） | `searchInput` / `searchGo` | ✅ search |
| `chips` | 标签组 | `chip` | ✅ search, settings |
| `list` | 列表（缩略图+标题+副标题+箭头） | `row` | ✅ search, download, history |
| `tabs` | **分段切换**（P1 新增） | `tab` | ✅ playlist |
| `grid` | **封面网格**（P1 新增） | `cell` | ✅ playlist |
| `songrow` | **歌曲行**（P1 新增） | `song` | ✅ playlist |
| `stat` | 大数字+柱状图 | — | ✅ stats, download |
| `bars` | 横向排行条 | — | ✅ stats |
| `toggle` | 开关列表 | `toggle` | ✅ download, settings |
| `now` | 旧播放器（竖列用） | `play` | ⛔ 已无空间使用 |
| `player` | 大播放器（竖列用） | `play` | ⛔ 已无空间使用 |
| `lyrics` | 歌词（竖列用） | — | ⛔ 已无空间使用 |
| `cover` | 圆角方形封面（裸） | — | ✅ now |
| `lyric3d` | 长图滚动歌词（裸，高亮） | — | ✅ now |
| `meta` | 歌曲信息（裸） | — | ✅ now |
| `nowinfo` | 作者·专辑一行（裸） | — | ✅ now |
| `transport` | 进度条+时间+控制（裸） | `seek` / `play` / `prev` / `next` | ✅ now |
| `queue` | 播放队列（裸） | — | ✅ now |
| `text` | 纯文本 | — | ✅ settings |

> **25 种全部有实现，无"用了没实现"的空白**。其中 `now` / `player` / `lyrics` 三种已无空间使用（竖列布局遗留）。
> 新增的 6 个图表/指标控件：`chart_bar24` / `chart_radar` / `chart_donut` / `chart_line` / `chart_heat` / `metrics`。

### D. 7 个空间的控件配置（校订版）

| 空间 | key | 布局 | 控件（按顺序） | 专属 cam/radius |
|---|---|---|---|---|
| 搜索 | `search` | 竖列 | `search` → `chips`(热门搜索) → `list`(搜索记录) | 默认 `CAM.sub` |
| **我的歌单** | `playlist` | **自由** | `tabs`(分类, az0/el24) → `grid`(歌单, az0/el-8) → `songrow`(歌曲, az42/el0) | `yawMax .95 / pitchMax .55 / sensX .0055 / sensY .005`, radius 7 |
| 听歌统计 | `stats` | 竖列 | `stat`(本周时长) → `bars`(歌手排行) → `bars`(歌曲排行) | 默认 |
| **当前播放** | `now` | **自由** | `lyric3d`(sf.29/sv.47, **tilt .42**) → `cover`(sf.76/sv.341, tilt -.32) → `nowinfo`(sf.76/sv.439) → `transport`(sf.76/sv.537) → `morebtn`(sf.76/sv.630) → `meta`(az-52/el12) → `playmenu`/`queue`/`playsetting`(球面) | 同上, radius 7 |
| 下载管理 | `download` | 竖列 | `stat` → `list`(下载中) → `toggle`(下载设置) | 默认 |
| 设置 | `settings` | 竖列 | `toggle`(外观) → `chips`(音质) → `toggle`(播放) → `text`(关于) | 默认 |
| 播放历史 | `history` | 竖列 | `list`(今天) → `list`(昨天) | 默认 |

> **歌单页的"同屏详情"**：`tabs` 切分组 → `syncPlaylistSet()` 换 `grid` 内容 + 首个歌单的歌曲列表；
> 点 `grid` 单元格 → `updateSongList()` 把右侧 `songrow` 换成该歌单的歌。
> **列表与详情同屏，不做两级**——这是 `docs/10` 的核心主张落地。

---

## 七、设计系统（真实色值，2026-09-23 外观打磨后更新）

> ⚠️ **已 token 化**：CSS 侧现为 `:root` 单一真相源；完整规范见 **`docs/15_外观打磨规范.md`**。
> 本节只列「现状速查」，改外观请看 docs/15。

```css
/* CSS 变量（app.html :root · 2026-09-23 重写为色板 token 区块）*/
--bg:      #08090C                     底色（clearColor/fog/theme-color 同步）
--bg-soft: #0D0E11                     次级底色
--t1:      #ECEDEF                     主文字（中性冷白）
--t2:      #8C9097                     次要文字（灰）
--t3:      #4B4E54                     辅助/占位（深灰）
--accent:  #7A828C                     强调（中性灰，非冷钢蓝！）
--glow:    rgba(122,130,140,0.32)      微光
--glass:   rgba(255,255,255,0.045)     玻璃卡片
--glass-b: rgba(255,255,255,0.08)      玻璃边框
--divider: rgba(255,255,255,0.06)      分隔线
--cool:    #93A7BE                     冷调微光（唯一允许的冷色，仅点缀）
--cool-dim:rgba(147,167,190,0.30)
/* 字号阶梯（对齐主项目 LXType）*/
--fs-xs:11px --fs-sm:13px --fs-body:14px --fs-bodyL:16px
--fs-title:18px --fs-headS:22px --fs-head:28px
```

**卡片底色 PALETTES（6 组中性灰）：**
```js
['#3A3D43','#1D1F23'], ['#3C3F45','#202226'], ['#353A41','#1B1D21'],
['#43464C','#23252A'], ['#393E46','#1E2024'], ['#3F4248','#212327']
```

**偏冷蓝的元素**（"冷蓝观感"的来源，但**不是主色**）：
| 位置 | 色值 | 说明 |
|---|---|---|
| 歌单封面配色 | `#3E4E58` / `#46525E` / `#3A4A5E` / `#4A5464` / `#544A5E` 等 | 卡片内封面渐变（由 `REAL.playlists` 驱动）—— **属内容数据，保留** |
| 曲目主题色 `TRACKS[].accent` | `96,126,158` / `90,96,124` / `122,106,106` | 播放页封面取色 —— **属内容数据，保留** |
| ~~选中态 chips~~ | ~~`rgba(50,90,150,0.36)`~~ | ✅ **已改中性** `rgba(255,255,255,0.11)` |
| ~~搜索面板按钮~~ | ~~`rgba(50,90,150,.30)`~~ | ✅ **已改中性玻璃** |
| ~~边缘指示箭头~~ | ~~`rgba(60,84,112,0.9)`~~ | ✅ **已改冷灰** `rgba(150,166,186,0.85)` |

> ⚠️ 另修复 3 处**浅色版迁移遗留 bug**（叠在近黑底上几乎不可见）：
> `.top .t` 深蓝渐变 `#22334E→#5E7290`、`.crumb` 深蓝文字、`.chip` 深蓝字与边框。

**电影感后期层：**
| 层 | 作用 |
|---|---|
| `.fx-grade` | 冷暖分级（左上冷白 → 右下深蓝） |
| `.fx-vignette` | 柔和暗角 |
| `.fx-leak` | 柔光斑（11s 呼吸） |
| `.fx-grain` | 胶片颗粒（0.7s 抖动） |

---

## 八、交互系统

### 转场（主页 ↔ 子空间）
```
点击卡片 → 卡片冲向镜头（0.40s，放大9倍）
        → 300ms 后压黑（.fx-wipe）
        → 470ms 切换场景 + 新控件从远处飞入（错落55ms）
        → 340ms 压黑解除
返回：压黑 → 260ms → 主页卡片飞回 → 视角还原（savedYaw）
```

### 射线命中（3D 空间里点控件）
```
pointer 坐标 → NDC → raycaster
   → 命中 mesh → hit.uv → 归一化坐标 (fx, fy)
   → 遍历 mesh.userData.regions → 触发 handleRegion(mesh, r)
```

### 拖拽（进度条）
```
pointerdown 命中 'seek' 区域 → seekDrag=true，按 UV.x seek
pointermove → 每帧重新射线拾取 → 实时 seek（期间不旋转相机）
pointerup → 结束
```

### 相机配置（每空间可覆盖，L2563）
```js
CAM = {
  main: { yawMax: Infinity, pitchMax: 1.30, sensX: 0.012, sensY: 0.008 },
  sub:  { yawMax: 0.22,     pitchMax: 0.72, sensX: 0.003, sensY: 0.006 }
}
// PAGES.xxx.cam 可覆盖（playlist / now 均覆盖为 yawMax:0.95）
```

### `handleRegion` 支持的 12 种 kind（L2741）
`searchInput` / `searchGo` / `chip` / `row` / `tab` / `cell` / `song` / `toggle` / `play` / `prev` / `next` / `seek`

### 底部迷你播放条（本次修复）
| 按钮 | 行为 |
|---|---|
| `#playBtn` | `setPlaying(!NP.playing)` |
| `#miniNext` | `switchTrack(1)` ← **2026-09-22 新增绑定**（此前是纯装饰，点了没反应） |

---

## 九、⚠️ 已知问题 & 死代码清单

### 已知问题
1. **播放页切歌时，底部迷你播放条**：`switchTrack()` 结尾**已调用 `syncMini()`**（L2440），
   在主页点迷你条「下一首」也能正确同步。
   > ⚠️ 旧版本记的「迷你条文字不更新」**已不成立**，2026-09-22 复核确认修复。
   > 但**在 `.np` DOM 播放器里**（死代码路径）走的是 `loadTrack()`，那条路也同步，只是不可达。
2. **旧文档过时**：`docs/06`（沉浸式歌曲页）已加废弃横幅；
   `docs/09` 的菜单弧章节已加废弃横幅（含 P0）。

### 死代码（可清理，不影响运行）

| 内容 | 位置 | 说明 |
|---|---|---|
| **环绕菜单墙** | `MENUS`(L1839) / `drawMenuGlyph`(L1850) / `makeMenuCard`(L1938) / `menuCards`(L1935) / `buildMenuWall`(L1967) | **L1987 调用被注释**，整块不可达 |
| **DOM 沉浸式播放器** | `.np` HTML(L316~362) + `.np-*` CSS + `openPlayer`(L2392) / `closePlayer`(L2400) | **`body.playing` 从未被添加**（`openPlayer` 无调用）→ 面板、`npPrev`/`npNext` 按钮全部不可达 |
| **3D 黑胶** | `makeVinylTexture`(L2186) / `makeLabelTexture`(L2213) / `coverGroup`(L2245) / `spinner` / `vinylDisc` / `labelMat` / `layoutVinyl`(L2274) | 只被 `openPlayer` 使用 → 随 DOM 播放器一起死 |
| **`CARD.love`** | L1651 | 收藏卡绘制函数，但 `FEATURES` 里**没有** `love` 键 → 无卡片调用 |
| **`loadTrack`** | L2371 | 只被 `openPlayer`(死) 和 `npPrev`/`npNext`(死) 调用 |
| **`npIdleT` / `pokeNP`** | L2509 / L2510 | 只服务于 `.np` 沉浸隐去，随 DOM 播放器一起死 |
| **DRAW 的 `now` / `player` / `lyrics`** | L1013 / L1072 / L1134 | 竖列布局遗留，已无 PAGES 使用 |
| `.np-*` CSS | L165~253 | `.sheet` 仍在用（搜索输入）；`.np-*` 停用 |

> ⚠️ **不要删**：`TRACKS` / `NP` / `fmt()` / `curLyricIndex()` / `setPlaying()` / `syncMini()` / `switchTrack()`
> **仍在使用**（播放页歌词与进度依赖）。

---

## 九·五、⚡ 玻璃化与形态重构（2026-09-23 新增）

> 起因：用户反馈「不够惊艳、超凡脱俗、设计不够新颖」。
> 诊断根因：所有元素用 `MeshBasicMaterial`（无光照/无反射）→ 像贴纸不像实体。

### 技术实现（三件套）

| 组件 | 实现 | 位置 |
|---|---|---|
| **程序化环境贴图** | `buildBrandEnvScene()`（深空球 + 5 片品牌色发光板）→ `PMREMGenerator.fromScene(envScene, 0.10)` | 见架构表「玻璃化地基」 |
| **光照系统** | `AmbientLight`(0x8891A0/0.55) + `DirectionalLight`(0xAEB8C8/0.90) + 2 盏品牌色 `PointLight` | 同上 |
| **玻璃材质** | `makeGlassMaterial()`：`MeshPhysicalMaterial` + clearcoat 0.85 + iridescence 0.22 + envMapIntensity 1.6 | 同上 |

### ⚠️ 三个关键取舍（踩过的坑）

1. **贴图面不能用 `transmission`** —— 否则贴图被折射冲淡（像透过玻璃看纸）。
   贴图面用「半透明 0.94 + 高反射 + clearcoat」；纯色侧边才用真折射。
2. **`BoxGeometry` 的实色侧边 = 硬边界** —— 斜看时露出，用户反馈"还有边界"。
   已全部改 `PlaneGeometry`（主页卡 + 子空间控件）。
3. **圆形擦除蒙版对矩形擦不干净** —— 实算主页卡上下边缘只擦掉 49%。
   已改「四边线性渐变 + 四角加强」（`edgeY` 比 `edgeX` 更大，因上下边缘离中心更近）。

### 可调参数

| 参数 | 位置 | 作用 |
|---|---|---|
| `GLASS_ON` | 玻璃化地基后 | 总开关（false = 退回哑光对比） |
| `GLASS_TRANSMISSION` | 同上 | 0 = 关闭真折射（省性能） |
| `DISSOLVE` | `drawCardBg` 前 | 溶解强度（0 = 旧实心卡片，1 = 完全溶解）当前 0.86 |
| `AUDIO.bpm` | 音频响应段 | 节拍（当前 92，Jazzy Hip-Hop 区间） |
| `AUDIO.enabled` | 同上 | 音频响应开关 |

### 技术约束

`three.min.js` 是 **r150 UMD 构建，不含 examples**
→ `EffectComposer` / `UnrealBloomPass` / `BokehPass` 等**不可用**
→ 泛光/景深/色差需**手写 shader** 或用 CSS 层替代

### 调研结论（已存证，避免重复调研）

| 项 | 结论 |
|---|---|
| 真玻璃 `transmission` | 移动端几十个物体跑不动；本项目 10~30 卡片可以（用户确认不卡） |
| 最佳参考 | [Codrops: Infinite Liquid Glass Grid](https://tympanus.net/codrops/2026/09/08/building-an-infinite-liquid-glass-grid-with-three.js-webgpu-and-tsl/) —— 自写 SDF 假玻璃（单 pass、零 render target） |
| "卡片变光片" | 去掉矩形边界 + SDF `smoothstep` 软边 + 关 depthWrite + AdditiveBlending |
| Bloom 参数 | **不糊的关键是 `threshold` 0.7~0.9**（不是降 strength） |
| GitHub 现状 | 「玻璃 UI + 3D 卡片环」交叉领域**无高星开源项目** |

---

## 九·六、⚡ 外观打磨（2026-09-23 新增）

> 完整规范见 **`docs/15_外观打磨规范.md`**（本文只列要点）。

### 四项打磨（全部完成）

| # | 项 | 结果 | 关键数据 |
|---|---|---|---|
| 1 | **色彩体系统一** | CSS `:root` 重写为 token 区块；清除 **130 处 JS + 9 处 CSS** 冷蓝残留 | 冷蓝从 95 处降至 7 处（均为有意点缀） |
| 2 | **数据可视化调色** | `chart_donut` 从 LX-Pro 高饱和 9 色改冷灰明度梯度 | HSV 0.56~0.96 → **0.04~0.26** |
| 3 | **字号阶梯** | JS **28 种系数 → 10 档**（`TS` 常量）；CSS **13 种固定字号 → 7 档** `var(--fs-*)` | 对齐主项目 `LXType` |
| 4 | **材质升级** | 手写 **Bloom 泛光**（5-pass）+ **液态玻璃**（SDF 软边/边缘折射/菲涅尔） | threshold 0.82；零额外 render target |

### 顺带修复的 3 个真 bug

`.top .t` 深蓝渐变 `#22334E→#5E7290`、`.crumb` 深蓝文字、`.chip` 深蓝字与边框
—— 均为**浅色版迁移遗留**，叠在近黑底 `#08090C` 上对比度约 1.5:1，几乎不可见。

### ⚠️ 关键技术事实（勿搞错）

1. `three.min.js` 是 **r160** UMD（文档旧写「r150」有误），**不含 examples**
   → 泛光必须手写（`EffectComposer`/`UnrealBloomPass` 不可用）。
2. 走泛光管线时渲染器**不再输出 sRGB**（渲染到 RT 是 linear）
   → 合成 pass 里**手动做 linear→sRGB 编码**。
3. 液态玻璃注入的折射采样**必须保留原 `#include <map_fragment>` 并追加**
   —— 替换会丢 `DECODE_VIDEO_TEXTURE` / alphaMap。
4. r160 里法线是**局部变量 `normal`**（`normal_fragment_begin` 定义），**不是 varying `vNormal`**。
5. **安全网**：着色器编译失败时 three 不抛异常（只 console.error，卡片变黑块）
   → 已挂 `renderer.debug.onShaderError` 钩子自动降级。

### 验证方式（本轮实际用的）

| 手段 | 结果 |
|---|---|
| `node test/run.js` | **261 项全过**（新增 5o 段 35 项） |
| geckodriver + WebDriver 真实渲染 | 控制台**零错误**、`liquidFailed:false`、6 卡全部注入液态玻璃 |
| 逐像素对比实验（开/关泛光/液态玻璃） | 关泛光后亮像素占比 **1.51%→0.85%**；关液态玻璃 26.7% 像素不同 —— **证明效果真生效** |

> ⚠️ **待真机确认**：headless firefox 走软件 WebGL（llvmpipe），
> PBR 的观感与帧率需项目作者在真机上看。卡顿可 `BLOOM.enabled=false` / `LIQUID.enabled=false`。

---

## 十、下一步计划

> 依据 `docs/09`（其第七节已按实际进度校订）

| 阶段 | 内容 | 需新增控件 | 状态 |
|---|---|---|---|
| ~~P0~~ | ~~环绕菜单~~ | — | ⛔ 已废弃 |
| **P1 尾** | 列表型空间剩余：**播放历史 / 下载 / 搜索增强**（排行） | 复用 `songrow` / `list` | ⬜ |
| **P2 尾** | 详情空间剩余：**专辑 / 歌手 / 相似歌曲**（头图+列表同屏） | `headcard`（头图卡） | ⬜ |
| **P3** | 播放页扩展：右转=更多操作 / 下转=播放设置 / 长按=操作环 | `opring` / `slider` | ⬜ |
| **P4** | 评论（做成播放页环绕面）+ 统计 + 设置丰富化 | `comment` | ⬜ |
| **P5** | 主页内容丰富化（今日统计 / 最近播放横滑） | — | ⬜ |

**建议下一期从「播放历史」开始**：它已有 `list` 控件基础，
改造成 `songrow`（序号+歌名+歌手+时长）即可与「我的歌单」风格统一，
且 `history` 卡片已在主页球面上（az 309）。

> ✅ **方向已定（项目作者 2026-09-22）**：球面空间感**就是要的**，不是备选；
> 且「**球面做骨架、液态玻璃做材质**」。但**暂不写入原生文档——先把原型做完再谈**。
> 所以：**放心在本原型上继续加功能**（第十四节只是背景与映射参考，不是阻塞项）。

---

## 十一、经验教训（踩过的坑，务必避免）

| # | 坑 | 教训 |
|---|---|---|
| 1 | `m.widget` 写成 `m.userData.widget` | three.js 自定义数据**必须挂 userData** |
| 2 | 元素各自漂浮（随机相位） | 间距忽大忽小 → **同组元素用 `float:0`**，氛围靠相机 |
| 3 | 菜单卡缺 `home` / `faceMat` | 转场时 `position.copy(undefined)` 崩溃 → **卡片工厂必须给 home + faceMat** |
| 4 | 手机 touchend 后系统补发 mouseup | 点击执行两次（开关弹回）→ **用时间戳去重**（`mouseIsSynthetic`） |
| 5 | 歌词长句被裁切 | 固定字号绘制会裁切 → **绘制前 measureText 自适应缩放** |
| 6 | 主页 yawMax 限制 ±π | 环形卡片卡在180°（正好是某张卡）→ **主页 yawMax = Infinity** |
| 7 | 渲染循环抛异常 | 画面卡死 + 交互失效 → **优先怀疑 animate 内报错** |
| 8 | **文档行号漂移** | 代码一改，行号索引整节失效 → **改代码顺手更新第四节表格** |
| 9 | **误判"漏调函数"** | `read_file` 分页截断会把函数结尾切掉 → **判断前先读全函数体** |
| 10 | **硬匹配不同来源的名字** | 歌单名（用户自取）vs 专辑名（平台）零交集 → 跳转永不触发。**匹配前先验证交集** |
| 11 | **只做正向路径** | 加了跳转却没加返回栈；加了滚动却只修裁切。**功能要成对实现（正+反）** |
| 12 | **改完未自测就推** | 多轮"报告修好了但实际没生效"。**改完先跑 `node test/run.js`** |
| 13 | **硬编码行号写文档** | 行号表已失效四次。**用 `test/linemap.py` 生成，别手写** |

---

## 十二、测试方法（新会话照做）

**本机现状（2026-09-22 核实）**：只有 `firefox`，无 `chromium`/`puppeteer`/`playwright`/`jsdom`。
本机内存约 3.7GB，**跑浏览器前先 `free -h`**——内存紧张时不要开浏览器（会卡死系统）。

**方案 A：静态检查（零风险，首选）**
```bash
cd prototype
# 1) 提取内联 JS 做语法检查（node 在 ~/.local/bin/node）
python3 -c "
import re
src=open('app.html',encoding='utf-8').read()
open('/tmp/app_check.js','w').write('\n'.join(re.findall(r'<script>(.*?)</script>', src, re.S)))
"
node --check /tmp/app_check.js

# 2) DOM id 闭环：JS 引用的 id 是否都在 HTML 里
python3 -c "
import re
src=open('app.html',encoding='utf-8').read()
js='\n'.join(re.findall(r'<script>(.*?)</script>', src, re.S))
html=src[:src.index('<script src=\"three.min.js\">')]
dom=set(re.findall(r'\bid=\"([^\"]+)\"', html)); used=set(re.findall(r\"getElementById\('([^']+)'\)\", js))
print('悬空 id 引用:', sorted(used-dom) or '无 ✅')
"
```

**方案 B：真实浏览器（内存充裕时）**
1. 打开 `prototype/app.html`（本地文件，或部署后的线上地址）
2. 看控制台（必须零 error）
3. 数值验证：卡片方位、控件区域命中、切歌同步

**⚠️ 注意**：`browser:evaluate` 对复杂箭头函数偶发语法错误 → 改用 `function(){}` 写法更稳。

---

## 十三、文档索引（含废弃标注）

| 文档 | 内容 | 状态 |
|---|---|---|
| `01_主页设计策划` | 初版主页设计（扁平版） | 参考（已过时） |
| `02_真3D空间感技术调研` | CSS 3D vs WebGL 的结论 | ✅ 有效（对应 `legacy/space*.html`） |
| `03_参数调节手册` | 早期 `space.html` 的参数 | 参考（**不是** `app.html`） |
| `04_主页导航结构` | 初版导航结构 | 参考（已过时，对应 `legacy/home.html`） |
| `05_页面结构与转场规范` | 转场机制 | ✅ 有效（数值已变，机制不变） |
| `06_沉浸式歌曲页规范` | DOM 播放器方案 | ⛔ **已废弃**（顶部有横幅） |
| `07_空间播放器规范` | 播放页布局 | ✅ 有效（部分尺寸已改） |
| `08_LX-Pro界面调研报告` | LX-Pro 全部界面与信息结构 | ✅ 有效 |
| `09_空间化建构方案` | 页面映射与分期计划 | 🟡 部分有效（菜单弧已废弃，进度已校订） |
| **`10_空间重组设计思想`** | ★★★ **设计哲学，最高优先级** | ✅ 有效 |
| **`11_项目交接文档`** | 本文件 | ✅ 有效 |
| **`12_部署与域名`** | Vercel + Cloudflare 反代链路、配置位置、踩坑 | ✅ 有效 |
| **`13_LX-Pro界面分析与重构方案`** | LX-Pro 界面全清单（含第三方/本地判定）+ 4 项重构方案 | ✅ 有效 |
| **`14_LX-Pro界面覆盖审计`** | ★ 逐项核对：哪些已重构/简版/未做/不移植 | ✅ 有效 |
| **`15_外观打磨规范`** | ★★★ **外观单一真相源**：色板 token / 字号阶梯 / 泛光 / 液态玻璃 | ✅ 有效 |

---

## 十四、与「Android 原生重写」的关系（⚠️ 重要，2026-09-22 重写）

> 项目作者 2026-09-22 确认：本原型用于**验证效果**，最终 App 用 **Android 原生重写**。

### 14.1 关键事实：原生项目已经存在，且路线已拍板

**不是"将来要重写"，而是"主项目已在另一目录"**：

| | 本工作区 | 目标项目 |
|---|---|---|
| 路径 | `~/Hoper/LX_Pro_Rebuild_UI` | `~/Hoper/LX_Pro_Rebuild` |
| 内容 | UI 原型（Three.js）+ docs 01~11 | 策划文档 01~11，**无代码** |
| 性质 | **视觉验证** | **真正要开发的 App** |

**已拍板的技术路线**（见 `LX_Pro_Rebuild/README.md`）：
- `Kotlin + Jetpack Compose + Media3`（纯原生，非 WebView）
- 包名 `com.lxpro.music`，minSdk **33**（Android 13）
- **只内置 Bilibili 音源**，其余由用户导入 JS 脚本
- 颠覆优先级：`性能与质感` > `交互范式` > `独家能力` > `AI`

### 14.2 两个工作区的「空间语言」差异（⚠️ 2026-09-22 已订正）

两个工作区各自描述了"外观"，**互不引用**（已 grep 确认：主项目 12 份文档里
没有一处提到 `LX_Pro_Rebuild_UI` / `app.html` / 球面 / 环绕 / 转动视角）。

| | 本工作区（UI 原型） | 主项目 `11-视觉设计体系.md` |
|---|---|---|
| 核心主张 | **真 3D 空间**：球面环绕、转动视角、裸元素浮在空间里 | **液态玻璃 + 空间深度**：Haze 毛玻璃、AGSL 折射、卡片层叠视差 |
| 实现路径 | Three.js / WebGL | Jetpack Compose `graphicsLayer` + Haze（**同为真透视**，见下方订正） |
| 空间的层级 | **架构层**：转视角 = 获取信息（无"页面"概念） | **装饰层**：卡片微倾 8° / 滚动视差 / 共享元素转场（**架构仍是页栈**） |
| 导航 | 6 张功能卡散布球面 | `内容层 NavHost`（首页/搜索/歌单/设置）+ 播放层覆盖 |
| 色彩 | 中性灰黑 `#08090C` / `#7A828C` | 见 `11` §4.3「更激进的取色」（封面取色驱动） |

> ⚠️ **订正（2026-09-22）**：本节初稿曾把差异说成「真 3D vs 伪 3D 的**技术冲突**」——
> **那是错的**，已修正：
>
> - **Compose 的 `graphicsLayer` 是真透视**：底层是 Android `RenderNode` 的 4×4 透视矩阵，
>   `cameraDistance`（单位**像素**，默认 `8*density`）驱动真实的透视除法，**不是伪 3D**。
> - 原生侧 `11-视觉设计体系.md` §3.2 自己就在用 `rotationX/Y + cameraDistance`（真透视），
>   所以「球面环绕」与「Compose + Haze」**技术上并不冲突**。
>
> **真正的差异不在技术，而在「空间的层级」**（见上表）：
> 原生侧的"空间深度"是**页内装饰**（倾斜/视差/转场），而原型的空间感是**导航架构**（转动即浏览）。
>
> **其余两点仍是真差异**：① 原生侧目前**没有球面交互**的设计（12 份文档零提及）；
> ② 本原型**没有实现液态玻璃**。
>
> **项目作者 2026-09-22 已定方向**：
> - 球面空间感**就是要的**（不是备选），且**球面做骨架、液态玻璃做材质**（两者融合）
> - 但**暂不写入原生文档**——**先把原型做完再谈**
>
> 因此本文档后续工作仍是：**在原型上把球面做完整**（P1 尾 / P2 尾）。
> 玻璃材质待原型定稿后一并写入原生设计。

### 14.3 技术概念映射（原型定稿后写入原生时用）

| 原型里的做法 | 作用 | Android 对应思路 |
|---|---|---|
| 相机 yaw/pitch + 球面坐标 | 转动视角浏览 | 手势 drag → 角度；`graphicsLayer { rotationY/rotationX }` + `cameraDistance` |
| `tilt` 固定倾斜 | 不跟随相机的透视感 | `graphicsLayer { rotationY = tilt }` |
| `m.lookAt(camera)`（billboard） | 始终正对镜头 | 不加旋转，天然正对 |
| 射线拾取 `raycaster` + `hit.uv` | 3D 里点控件 | **难点**：需把控件位置投影到屏幕坐标做矩形命中测试 |
| `bare:true`（PlaneGeometry） | 裸元素，无底板 | 无背景的 Compose `Text`/`Image` |
| 非 bare（`BoxGeometry` 0.06 厚） | 有厚度的卡片 | 改用 **Haze 玻璃卡片**（原生已有方案） |
| `PlaneGeometry` + `CanvasTexture` | Canvas 画 2D 当贴图 | **直接用 Compose 组件**（省掉贴图烘焙，可交互性更好） |
| `tex.offset.y` 滚动歌词 | GPU 侧零重绘滚动 | `LazyColumn` + `graphicsLayer` 平移 |
| 每帧改 `mesh.scale/position` | 漂浮/入场动画 | `animateFloatAsState` / `Animatable` |

> ✅ **技术判断（2026-09-22 订正）**：Compose 的 `graphicsLayer` **能做真透视**
> （`RenderNode` 4×4 矩阵 + `cameraDistance` 驱动透视除法），**不需要 Filament**。
> 球面环绕若移植到原生，用 `graphicsLayer` 自算投影即可（几十张平面卡片规模足够）。
>
> **唯一实质局限**：Compose 是 2D 合成器，**无深度缓冲**——
> 卡片间遮挡只能靠 `zIndex` 按 Z 排序后依次绘制，无法逐像素正确遮挡。
> 另外每个卡片是独立 Layer，各自有 `transformOrigin`/`cameraDistance`，
> 需统一 pivot 到屏幕相机心，否则会出现"各自的消失点"。
>
> 只有需要**真实几何体/曲面、逐像素遮挡、光照/PBR、上千 draw call** 时才需 Filament。
> **几十张平面卡片绕球面转动 —— `graphicsLayer` 足够。**

### 14.4 本原型相对原生设计的「增量价值」

方向已定（球面做骨架、玻璃做材质），本原型提供了这些**原生文档里没有的**东西：

| # | 增量 | 说明 |
|---|---|---|
| 1 | **6 张功能卡的球面坐标** | az/el + 尺寸（本文第六节 A），可直接换算 |
| 2 | **25 种控件的绘制规格** | 每种控件的排版系数（`0.22*P` 一类）与可点区域定义 |
| 3 | **空间播放器的同屏布局** | 歌词左 / 封面右 / 控制下（sf/sv 精确比例） |
| 4 | **「列表与详情同屏」的落地范例** | 歌单页 tabs+grid+songrow 三者环绕，不跳页 |
| 5 | **转场时序** | 0.40s 卡片冲向镜头 → 300ms 压黑 → 470ms 切换 → 错落 55ms |
| 6 | **真实色值表** | 本文第七节（从代码抄录，非臆测） |

> ⚠️ 注意：主项目 `11-视觉设计体系.md` §7 已有**设计令牌**（玻璃/深度/动效令牌），
> 本原型的色值**不要**直接覆盖它——两者是不同视觉体系。
> 按已定方向「球面做骨架、玻璃做材质」，应做的是**合并**：
> 球面骨架用本原型的坐标/布局，材质用原生已有的玻璃令牌。

### 14.5 待补规格（原型定稿后、写入原生前需做）

| # | 缺什么 | 现状 |
|---|---|---|
| 1 | **本原型的色值表** | 已有（本文第七节）。但色值也散在 JS 硬编码 rgba（80+ 处）里，未完全抽取 |
| 2 | **字号/间距/圆角标尺** | 现在全靠 `P * 系数` 现算（如 `0.22*P`），无可读标尺 |
| 3 | **逐控件字段签名** | 本文第六节 C 有清单与 kind，缺完整字段默认值 |
| 4 | **相机参数全表** | 7 个空间中仅 `playlist`/`now` 有专属 cam，其余用默认 `CAM.sub` |

> **不要**盲目产出「原生重写规格」——先解决 14.2 的口径问题，
> 否则会做出一份与主项目设计体系打架的文档。


---

## 附：快速定位（app.html 真实行号，2026-09-23 外观打磨后 · 共 9671 行）

| 内容 | 行号 |
|---|---|
| CSS 变量 / 后期层 | 10~52 |
| HTML 结构 | 287~391 |
| **字号阶梯 TS** | **413** |
| **真实数据 REAL** | **465~4520** |
| **泛光后处理 BLOOM** | **4115** |
| **液态玻璃 LIQUID / applyLiquidGlass** | **4383 / 4392** |
| FEATURES / SPOTS | 4522 / 4532 |
| PAGES 配置（9 空间） | 4683~5085 |
| PALETTES | 5086 |
| **DRAW 控件绘制（38 种）** | **5214~6791** |
| `widgetHeight` | 6792 |
| CARD 主页卡片绘制 | 6825~7276 |
| 主页卡片创建（R=5.6） | 7294~7348 |
| ⛔ 环绕菜单墙（死） | 7349~7558 |
| **长列表核心** | **7559~8037** |
| 子空间控件系统 | 7560~8246 |
| TRACKS | 8247~8343 |
| ⛔ 3D 黑胶 / DOM 播放器（死） | 8344~8509 |
| 空间播放器 | 8510~8618 |
| **音频响应（AUDIO）** | 8619~8707 |
| 状态与相机 CAM | 8708~8760 |
| 射线 / 交互 | 8761~8982 |
| handleRegion | 9011 |
| **导航栈 gotoSpace** | 9298~9441 |
| 动画循环 animate（含 BLOOM.render） | 9529~9649 |
| 启动 / UI | 9426 / 9650~9671 |

---

## 附二：行号维护方法（改代码后照做）

行号会随代码漂移。**改完大段代码后**，跑一行命令重新生成：

```bash
cd prototype && python3 test/linemap.py
```

它会输出全部锚点的新行号（`--md` 参数额外输出 Markdown 表）。
本文件的第四节目录表与上方快速定位表均据此更新。

> ⚠️ **教训**：行号表已失效五次（3150 → 3672 → 5114 → 5482 → 9196 → 9601）。
> 根因都是「改完代码忘了同步文档」。**改代码 = 顺手更新行号**，
> 或用上面的脚本一键重生成。

---

## 附三：测试与数据

```bash
# 回归测试（改完 app.html 必须先跑）
cd prototype && node test/run.js     # 261 项断言，退出码 0 = 全过

# 外观相关（泛光/色彩/字号/液态玻璃）另见 docs/15 §六：
#   python3 tools/make_diag_copy.py   # 生成带探针的 _diag.html
#   python3 tools/wd_verify.py        # geckodriver 真实渲染验证
#   python3 tools/verify_visual.py    # 4 变体逐像素对比

# 数据更新后重新生成
python3 data/gen_real_data.py > data/real_data.js
# 然后内联进 app.html（替换 "const REAL = {};" 到 REAL.recent 块）
```

- `prototype/test/` —— 回归测试套件（harness.js 提供 THREE+DOM 桩）
- `data/` —— 真实数据源 + 生成脚本（原始 lxmc 已 gitignore）
